spring.datasource.url=jdbc:postgresql://localhost:5432/eventclub_db
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:mvpr2586}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT config (set JWT_SECRET env variable in production)
jwt.secret=${JWT_SECRET:mysecretkeymysecretkeymysecretkey123456}
jwt.expiration.ms=3600000
